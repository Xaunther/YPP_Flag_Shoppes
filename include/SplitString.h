//Couple of fractions to split strings according to some delimiter

#ifndef splitstring_h
#define splitstring_h

#include <vector>
#include <string>

std::vector<std::string> SplitString(std::string s, std::string delimiter);
int CountString(std::string s, std::string pattern);

#endif