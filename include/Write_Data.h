//Function used to dump webpage into a file
//This is the template the function must have

#ifndef write_data_h
#define write_data_h
size_t Write_Data(void *ptr, size_t size, size_t nmemb, void *stream);

#endif